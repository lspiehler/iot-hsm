const projects = require('./projects');
const locations = require('./locations');
const keyrings = require('./keyrings');
const tokens = require('./tokens');

module.exports = {
  projects: projects,
  locations: locations,
  keyrings: keyrings,
  tokens: tokens
};